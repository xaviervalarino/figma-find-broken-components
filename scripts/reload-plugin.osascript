#!/usr/bin/env osascript

if application "Figma" is running then
  activate application "Figma"
    tell application "System Events"
        keystroke "p" using {command down, option down}
        delay 0.3
        keystroke tab using command down
    end tell
end if
